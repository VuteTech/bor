// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.1
// source: dconf.proto

/* eslint-disable */

export const protobufPackage = "bor.policy.v1";

/** DConfEntry represents one GSettings key to enforce. */
export interface DConfEntry {
  /** e.g. "org.gnome.desktop.screensaver" */
  schema_id: string;
  /** dconf path override; empty for non-relocatable schemas */
  path: string;
  /** e.g. "lock-enabled" */
  key: string;
  /** GVariant text format: "true", "'Adwaita'", "uint32 300" */
  value: string;
  /** add this key to the locks file (user cannot override) */
  lock: boolean;
}

/** DConfPolicy is a list of GSettings settings to enforce on a node. */
export interface DConfPolicy {
  entries: DConfEntry[];
  /**
   * db_name is the dconf system-db name. Defaults to "local" if empty,
   * writing into /etc/dconf/db/local.d/.
   */
  db_name: string;
}

/** GSettingsEnumValue describes one value of a GSettings enum or flags type. */
export interface GSettingsEnumValue {
  /** short identifier used in GVariant text, e.g. "prefer-dark" */
  nick: string;
  /** integer value */
  value: number;
}

/**
 * GSettingsKey describes one key within a GSettings schema, including all
 * metadata read from the schema XML on the agent node.
 */
export interface GSettingsKey {
  /** key name, e.g. "lock-enabled" */
  name: string;
  /** GVariant type code: "b", "s", "u", "i", "d", "as", … */
  type: string;
  /** short human-readable label  (from <summary>) */
  summary: string;
  /** long human-readable explanation (from <description>) */
  description: string;
  /** GVariant text format default, e.g. "true", "'Adwaita'" */
  default_value: string;
  /** For enum / flags types (<enum> or <flags> element in the schema XML). */
  enum_values: GSettingsEnumValue[];
  /** For numeric types with a <range> constraint. */
  range_min: string;
  /** GVariant text, e.g. "4294967295" */
  range_max: string;
  /** For string keys with a fixed set of <choices>. */
  choices: string[];
}

/** GSettingsSchema describes one schema available on the reporting node. */
export interface GSettingsSchema {
  /** e.g. "org.gnome.desktop.screensaver" */
  schema_id: string;
  /** fixed dconf path; empty if relocatable */
  path: string;
  /** true when path is not fixed */
  relocatable: boolean;
  keys: GSettingsKey[];
}

/**
 * ReportSchemaCatalogueRequest is sent by the agent at startup (and after
 * package installs) to inform the server which GSettings schemas are
 * available on this node.  The agent reads
 * /usr/share/glib-2.0/schemas/*.gschema.xml to build the list.
 */
export interface ReportSchemaCatalogueRequest {
  client_id: string;
  schemas: GSettingsSchema[];
  /** Informational: GNOME version string if detectable, e.g. "46". */
  gnome_version: string;
}

export interface ReportSchemaCatalogueResponse {
}

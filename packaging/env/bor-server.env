# Bor Policy Management Server — environment configuration
# Loaded by bor-server.service via EnvironmentFile=
# Protect with: chmod 640 /etc/bor/server.env && chown root:bor /etc/bor/server.env

# ── Database ──────────────────────────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_USER=bor
DB_PASSWORD=change-me
DB_NAME=bor
DB_SSLMODE=disable

# ── Server ────────────────────────────────────────────────────────────────────
BOR_ADDR=:8443

# ── Security ──────────────────────────────────────────────────────────────────
JWT_SECRET=change-me-in-production

# ── PKI (auto-generated under /var/lib/bor/pki if not set) ───────────────────
# BOR_CA_CERT_FILE=
# BOR_CA_KEY_FILE=
# BOR_CA_AUTOGEN_DIR=/var/lib/bor/pki/ca
# BOR_TLS_CERT_FILE=
# BOR_TLS_KEY_FILE=
# BOR_TLS_AUTOGEN_DIR=/var/lib/bor/pki/ui

# ── LDAP (optional) ───────────────────────────────────────────────────────────
# LDAP_ENABLED=false
# LDAP_HOST=ldap.example.com
# LDAP_PORT=389
# LDAP_USE_TLS=false
# LDAP_BIND_DN=cn=readonly,dc=example,dc=com
# LDAP_BIND_PASSWORD=secret
# LDAP_BASE_DN=dc=example,dc=com
# LDAP_USER_FILTER=(uid=%s)
# LDAP_ATTR_USERNAME=uid
# LDAP_ATTR_EMAIL=mail
# LDAP_ATTR_FULL_NAME=cn

# Package builds — everything is generated, EXCEPT the tracked placeholder
# builds/agent-repo/.gitkeep, which keeps the Containerfile's
# `COPY builds/agent-repo/` valid on checkouts that never assembled the repo.
/builds/*
!/builds/agent-repo
/builds/agent-repo/*
!/builds/agent-repo/.gitkeep

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
go.work
/vendor/
/dist/
/build/
server/server
server/cmd/server/server
agent/bor-agent

# IDEs
.vscode/*
.idea/
*.swp
*.swo
*~
.DS_Store

# Database
*.db
*.sqlite
*.sqlite3

# Logs
*.log
logs/
tmp/
dev/syslog-ng/logs/

#AI Tools
CLAUDE.md

# Environment (covers .env, .env.local, .env-old, etc. — keep the example)
.env*
!.env.example

# Secrets / credentials (must never be committed)
sshkey
sshkey.*
*.pem
*.key
id_rsa*
id_ed25519*
*.keytab
dev/

# Node modules (for frontend)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Frontend build
server/web/frontend/dist/
server/web/frontend/build/
server/web/frontend/.cache/

# Source maps in static folder
server/web/static/*.map
server/web/static/*.LICENSE.txt

# Test coverage
*.coverprofile
coverage/
*.cover
*.coverage

# OS
Thumbs.db
